# Arduino Sensors and Modules Categorized Cheatsheet

This cheatsheet organizes sensors and modules for Arduino by category (e.g., Environmental Sensing, Motion Sensing, Communication, Display, Actuators, Storage) for quick reference. It details their purpose, use cases, specifications, libraries, and example code snippets. Designed for engineering students working on embedded systems.

## Environmental Sensing

| Sensor/Module         | Purpose                                  | When to Use                              | Key Specs                              | Library/Connection                     | Example Code Snippet                   |
|-----------------------|------------------------------------------|------------------------------------------|----------------------------------------|----------------------------------------|----------------------------------------|
| **DHT11/DHT22**       | Temperature & Humidity                   | Weather stations, HVAC control           | Temp: 0-50°C (DHT11), -40-80°C (DHT22); Humidity: 20-80% | `DHT.h` | ```c<br>#include <DHT.h><br>#define DHTPIN 2<br>#define DHTTYPE DHT11<br>DHT dht(DHTPIN, DHTTYPE);<br>void setup() { dht.begin(); }<br>void loop() { float t = dht.readTemperature(); float h = dht.readHumidity(); }<br>``` |
| **TMP36**             | Temperature                              | Simple temp monitoring                  | -40 to +125°C, Analog output           | None (AnalogRead) | ```c<br>void loop() { int val = analogRead(A0); float temp = (val * 5.0 / 1024.0) * 100.0 - 50.0; }<br>``` |
| **BME280**            | Temp, Humidity, Pressure                 | Advanced weather stations               | I2C/SPI, high precision               | `Adafruit_BME280.h` | ```c<br>#include <Adafruit_BME280.h><br>Adafruit_BME280 bme;<br>void setup() { bme.begin(); }<br>void loop() { float t = bme.readTemperature(); float p = bme.readPressure(); }<br>``` |
| **MQ Series (e.g., MQ-2)** | Gas Detection (Smoke, CO, etc.)      | Air quality, safety systems             | Analog output, gas-specific            | None (AnalogRead) | ```c<br>void loop() { int gasValue = analogRead(A0); if (gasValue > 300) { /* Alert */ } }<br>``` |
| **LDR (Photocell)**   | Light Intensity                          | Light-dependent control (e.g., lamps)   | Analog, resistance-based               | None (AnalogRead) | ```c<br>void loop() { int light = analogRead(A0); if (light < 500) { /* Dark */ } }<br>``` |

## Motion and Proximity Sensing

| Sensor/Module         | Purpose                                  | When to Use                              | Key Specs                              | Library/Connection                     | Example Code Snippet                   |
|-----------------------|------------------------------------------|------------------------------------------|----------------------------------------|----------------------------------------|----------------------------------------|
| **HC-SR04**           | Ultrasonic Distance                      | Obstacle detection, robotics            | 2-400 cm, 4-pin (Trig, Echo, Vcc, Gnd) | `NewPing.h` (optional) | ```c<br>#define trigPin 9<br>#define echoPin 10<br>void setup() { pinMode(trigPin, OUTPUT); pinMode(echoPin, INPUT); }<br>void loop() { digitalWrite(trigPin, HIGH); delayMicroseconds(10); digitalWrite(trigPin, LOW); long duration = pulseIn(echoPin, HIGH); float distance = duration * 0.034 / 2; }<br>``` |
| **PIR Sensor**        | Motion Detection                         | Security systems, automation            | Digital output, 5-20m range            | None (DigitalRead) | ```c<br>#define pirPin 2<br>void setup() { pinMode(pirPin, INPUT); }<br>void loop() { if (digitalRead(pirPin) == HIGH) { /* Motion detected */ } }<br>``` |
| **MPU6050**           | Accelerometer & Gyroscope                | Motion tracking, drone stabilization    | 3-axis accel, 3-axis gyro, I2C         | `Wire.h`, `Adafruit_MPU6050.h` | ```c<br>#include <Adafruit_MPU6050.h><br>Adafruit_MPU6050 mpu;<br>void setup() { mpu.begin(); }<br>void loop() { sensors_event_t a, g, temp; mpu.getEvent(&a, &g, &temp); }<br>``` |
| **IR Sensor**         | Line Following, Proximity                | Line-following robots, object detection | Analog/Digital output                 | None or `IRremote.h` (for IR remotes) | ```c<br>void loop() { int irValue = analogRead(A0); if (irValue < 200) { /* Black line */ } }<br>``` |

## Communication

| Module                | Purpose                                  | When to Use                              | Key Specs                              | Library/Connection                     | Example Code Snippet                   |
|-----------------------|------------------------------------------|------------------------------------------|----------------------------------------|----------------------------------------|----------------------------------------|
| **Bluetooth (HC-05)** | Wireless Communication                   | Remote control, data transfer           | Serial, 9600 baud default             | `SoftwareSerial.h` | ```c<br>#include <SoftwareSerial.h><br>SoftwareSerial bt(10, 11);<br>void setup() { bt.begin(9600); }<br>void loop() { bt.println("Data"); }<br>``` |
| **WiFi (ESP8266)**    | Internet Connectivity                    | IoT, cloud data logging                 | Serial or SPI, AT commands or native   | `ESP8266WiFi.h` | ```c<br>#include <ESP8266WiFi.h><br>void setup() { WiFi.begin("SSID", "pass"); }<br>void loop() { if (WiFi.status() == WL_CONNECTED) { /* Connected */ } }<br>``` |

## Display

| Module                | Purpose                                  | When to Use                              | Key Specs                              | Library/Connection                     | Example Code Snippet                   |
|-----------------------|------------------------------------------|------------------------------------------|----------------------------------------|----------------------------------------|----------------------------------------|
| **LCD (16x2, HD44780)** | Display Text/Numbers                   | User interfaces, data display           | 16x2 or 20x4, parallel or I2C          | `LiquidCrystal.h` or `LiquidCrystal_I2C.h` | ```c<br>#include <LiquidCrystal.h><br>LiquidCrystal lcd(12, 11, 5, 4, 3, 2);<br>void setup() { lcd.begin(16, 2); lcd.print("Hello, Arduino!"); }<br>``` |
| **OLED (SSD1306)**    | Graphical Display                        | Compact displays, graphs                | 128x64 pixels, I2C                    | `Adafruit_SSD1306.h` | ```c<br>#include <Adafruit_SSD1306.h><br>Adafruit_SSD1306 display(128, 64);<br>void setup() { display.begin(SSD1306_SWITCHCAPVCC, 0x3C); display.drawLine(0, 0, 127, 63); display.display(); }<br>``` |

## Actuators

| Module                | Purpose                                  | When to Use                              | Key Specs                              | Library/Connection                     | Example Code Snippet                   |
|-----------------------|------------------------------------------|------------------------------------------|----------------------------------------|----------------------------------------|----------------------------------------|
| **Servo Motor (SG90)** | Precise Angular Control                  | Robotics, mechanical movement           | 0-180°, PWM control                   | `Servo.h` | ```c<br>#include <Servo.h><br>Servo myServo;<br>void setup() { myServo.attach(9); }<br>void loop() { myServo.write(90); }<br>``` |
| **Stepper Motor (28BYJ-48)** | Step-Based Rotation                | Precise positioning (e.g., 3D printers) | 4 or 5-pin, ULN2003 driver            | `Stepper.h` | ```c<br>#include <Stepper.h><br>Stepper stepper(2048, 8, 9, 10, 11);<br>void setup() { stepper.setSpeed(10); }<br>void loop() { stepper.step(512); }<br>``` |
| **Relay Module**      | High-Power Switching                    | Control appliances, lights              | 5V control, up to 250V AC/10A         | None (DigitalWrite) | ```c<br>#define relayPin 7<br>void setup() { pinMode(relayPin, OUTPUT); }<br>void loop() { digitalWrite(relayPin, HIGH); }<br>``` |

## Storage

| Module                | Purpose                                  | When to Use                              | Key Specs                              | Library/Connection                     | Example Code Snippet                   |
|-----------------------|------------------------------------------|------------------------------------------|----------------------------------------|----------------------------------------|----------------------------------------|
| **SD Card Module**    | Data Logging                            | Store sensor data, configs              | SPI interface                        | `SD.h` | ```c<br>#include <SD.h><br>void setup() { SD.begin(10); }<br>void loop() { File file = SD.open("data.txt", FILE_WRITE); file.println("Data"); file.close(); }<br>``` |
| **EEPROM**            | Persistent Storage                       | Save settings, calibration data         | 1KB (Uno), internal                   | `EEPROM.h` | ```c<br>#include <EEPROM.h><br>void setup() { EEPROM.write(0, 42); }<br>void loop() { int val = EEPROM.read(0); }<br>``` |

## Selection Guide
- **Environmental Sensing**: DHT11 (budget), BME280 (precision), MQ-2 (gas), LDR (light).
- **Motion/Proximity**: HC-SR04 (distance), PIR (motion), MPU6050 (orientation), IR (line/proximity).
- **Communication**: HC-05 (short-range Bluetooth), ESP8266 (WiFi/IoT).
- **Display**: LCD (text), OLED (graphics).
- **Actuators**: Servo (angular), Stepper (steps), Relay (power switching).
- **Storage**: SD Card (large data), EEPROM (small persistent data).

## Quick Tips
- **Power**: Match 5V/3.3V requirements. Use external power for actuators (e.g., motors).
- **Pin Conflicts**: Avoid Serial pins (0, 1). Use PWM pins (3, 5, 6, 9, 10, 11 on Uno) for `analogWrite`.
- **Libraries**: Install via Arduino IDE Library Manager. Include `#include <Library.h>`.
- **Debugging**: Monitor sensor values with `Serial.println()`.
- **Interfacing**: Use I2C/SPI for complex modules, analog/digital for simple sensors.

## Example: Motion-Triggered Relay
```c
#define pirPin 2
#define relayPin 7
void setup() {
    pinMode(pirPin, INPUT);
    pinMode(relayPin, OUTPUT);
}
void loop() {
    if (digitalRead(pirPin) == HIGH) {
        digitalWrite(relayPin, HIGH); // Activate relay
    } else {
        digitalWrite(relayPin, LOW);
    }
}
```

## Resources
- Arduino Reference: `https://www.arduino.cc/reference/en/`
- Adafruit Tutorials: `https://learn.adafruit.com/`
- Datasheets: SparkFun, Adafruit product pages for pinouts and specs.
